PORT=3000
# Salesforce Connected App
SF_LOGIN_URL=https://login.salesforce.com
SF_CLIENT_ID=
SF_CLIENT_SECRET=
SF_REDIRECT_URI=https://localhost:3000/auth/sf/callback
SF_SCOPES=api refresh_token openid profile
# Encryption key for cookie session (change in prod)
SESSION_SECRET=change-me
# Allowed origins for CORS if you host add-in elsewhere
ALLOWED_ORIGIN=https://localhost:3000
